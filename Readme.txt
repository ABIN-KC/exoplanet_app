1)Create an Ensemble model based on dataset keplar given by Nasa
2)Create a web interface that allows the user to input.
3.1) either local host(change in index){ONLY ACCESIBLE USING THE LAP}
     give cd filename ; python app.py;
3.2) or using render and firebase(give render link in index.html){accessible for all}
     must use GitHub to connect with render
     give firebase login; firebase deploy