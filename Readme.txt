1)Create an Ensemble model based on dataset keplar given by Nasa
2)Create a web interface that allows the user to input.
3) either local host(change in index){ONLY ACCESIBLE USING THE LAP}
  3.1) give cd filename ; python app.py;
4) or using render and firebase(give render link in index.html){accessible for all}
  4.1) must use GitHub to connect with render
5) use godaddy